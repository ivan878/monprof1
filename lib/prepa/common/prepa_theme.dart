import 'package:flutter/material.dart';

// ── Couleurs Prepa Concours ──────────────────────────────────────────────────
// NOTE : ne pas modifier `primaryColor` global (bleu clair du reste de l'app).
// Ces constantes sont UNIQUEMENT pour le module Prepa.

const Color prepaPrimaryColor = Color(0xFFD32F2F); // Rouge Prepa
const Color prepaPrimaryLight = Color(0xFFFF6659);
const Color prepaPrimaryDark = Color(0xFF9A0007);
const Color prepaPrimaryBg = Color(0xFFFFEBEE); // fond rouge très clair
